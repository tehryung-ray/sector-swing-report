# 섹터 스윙 리포트 웹앱 — 구축 계획서

마스터기획서 4단계(웹 대시보드 및 자동화)의 실행 설계.
정적 사이트(GitHub Pages) + 스케줄 파이프라인(GitHub Actions) 구조.

---

## 0. 설계 원칙

| 원칙 | 결정 | 이유 |
|---|---|---|
| 빌드 없는 정적 사이트 | 순수 HTML/CSS/JS + JSON | GitHub Pages와 마찰 0, npm 빌드 실패로 아침 리포트가 안 뜨는 사고 방지 |
| 데이터와 화면 분리 | 파이썬이 JSON만 생성, JS가 렌더 | 로직 수정과 UI 수정이 서로를 깨뜨리지 않음 |
| 모바일 우선 | 카드 레이아웃 기본, 표는 데스크톱에서 | 아침 7~8시 확인은 대부분 휴대폰 |
| 원본 데이터 캐시 | OHLCV를 리포에 커밋 | 데이터 소스가 하루 죽어도 리포트는 생성됨 |
| 실패는 조용히 넘어가지 않음 | 종목 코드 미해결·데이터 결측 시 워크플로 실패 | 잘못된 가격을 표시하는 것보다 안 뜨는 게 나음 |

---

## 1. 시간표 (기획서 2장 구체화)

미국 장 마감 → 한국 장 개장 사이의 실제 UTC 시각:

| 이벤트 | 서머타임(EDT) | 겨울(EST) |
|---|---|---|
| 미국 정규장 마감 16:00 ET | 20:00 UTC (05:00 KST) | 21:00 UTC (06:00 KST) |
| **파이프라인 실행** | **21:50 UTC (06:50 KST)** | 동일 |
| **재시도 실행(1차 실패 시)** | **22:50 UTC (07:50 KST)** | 동일 |
| 한국 장 개장 09:00 KST | 00:00 UTC | 동일 |

- 21:50 UTC 한 시각이면 서머타임/겨울 양쪽 모두 미국 마감 후이므로 **크론 하나로 연중 커버**된다.
- **크론 요일은 `0-4`(UTC 일\~목)** 이다. UTC 21:50 은 KST 다음날 06:50 이므로,
  `1-5`(UTC 월\~금)로 쓰면 KST 화\~토가 되어 **월요일 리포트가 빠지고 토요일에 헛돈다.**
- GitHub Actions 크론은 최대 15분 이상 지연되는 것이 정상이므로 22:10 KST 07:10 도착을 목표로 여유를 둔다.
- 2차 실행은 `latest.json`의 날짜가 오늘이면 즉시 종료(중복 커밋 방지).

**중요한 전제**: 오전 7시 시점에 한국 ETF의 "오늘" 가격은 존재하지 않는다.
따라서 진입/익절/손절가는 모두 **전 거래일 종가 기준**으로 계산된 값이며,
리포트에도 "기준일: 2026-09-05 종가" 를 명시한다.

---

## 2. 리포지토리 구조

```
sector_invest/
├─ .github/workflows/daily.yml      # 크론 + 수동 실행(workflow_dispatch)
├─ config/
│  ├─ us_sectors.yaml               # 미국 섹터 ETF 유니버스
│  └─ kr_universe.yaml              # 한국 ETF + 매핑 + 링크 (기획서 2단계 "지도")
├─ pipeline/
│  ├─ main.py                       # 오케스트레이션
│  ├─ sources/
│  │  ├─ us.py                      # yfinance
│  │  └─ kr.py                      # FDR → pykrx → yfinance 3단 폴백
│  ├─ resolve_codes.py              # ETF 이름 → 6자리 코드 검증
│  ├─ momentum.py                   # 미국 섹터 랭킹 (기획서 1단계)
│  ├─ levels.py                     # 지지/저항/진입/익절/손절 (기획서 3단계)
│  ├─ gates.py                      # 관망 판정 필터
│  └─ render.py                     # JSON 출력
├─ cache/                           # OHLCV parquet, 리포에 커밋
├─ docs/                            # ← GitHub Pages 루트
│  ├─ index.html
│  ├─ assets/{app.js, style.css}
│  └─ data/
│     ├─ latest.json                # 오늘 리포트
│     ├─ archive.json               # 과거 날짜 목록
│     └─ 2026-09-08.json            # 일자별 스냅샷
├─ tests/test_levels.py             # 가격 계산 단위 테스트
└─ requirements.txt
```

GitHub Pages 설정: **Settings → Pages → Source: `main` 브랜치 / `/docs` 폴더**.
(gh-pages 브랜치 배포보다 단순하고, 데이터 커밋과 사이트가 같은 커밋에 들어감)

---

## 3. 데이터 소스와 폴백

### 미국 (yfinance)
섹터 ETF 종가/거래량. 마감 직후 안정적.

### 한국 — 3단 폴백 (이게 이 프로젝트 최대 리스크)
GitHub Actions 러너는 **미국 IP**다. `data.krx.co.kr`(pykrx)은 해외 IP에서
차단되거나 극단적으로 느려질 수 있다. 그래서:

1. `FinanceDataReader` (네이버 소스) — 1순위
2. `pykrx` — 2순위
3. `yfinance` 의 `069500.KS` 형식 — 3순위 (거래대금은 못 얻지만 OHLCV는 확보)

세 개가 다 실패하면 → `cache/` 의 직전 데이터로 리포트를 생성하되
**"데이터 갱신 실패 · 기준일 N일 전" 경고 배너**를 리포트 상단에 강제 노출.

### 종목 코드 검증
6자리 ETF 코드를 하드코딩하면 상장폐지·티커 변경 시 조용히 틀린 값이 나온다.
`resolve_codes.py` 가 매 실행마다 FDR의 ETF 목록에서 **이름 → 코드**를 대조하고,
불일치 시 워크플로를 실패시킨다. `kr_universe.yaml` 의 코드는 시드값으로만 취급.

---

## 4. 계산 로직 (기획서 3단계)

### 4-1. 미국 섹터 랭킹
각 미국 섹터 ETF에 대해:

```
mom_score = 0.5 × (5일 수익률)
          + 0.3 × (20일 수익률)
          + 0.2 × (1일 수익률)
RS        = 섹터 20일 수익률 − SPY 20일 수익률
추세       = 종가 > 20MA and 20MA > 60MA
```
- `🟢 강한 상승` : mom_score 상위 30% + RS > 0 + 추세 True
- `🟡 상승 전환` : 종가가 20MA를 최근 3일 내 상향 돌파
- `🔴 하락세`   : 종가 < 20MA

**시장 국면 필터**: SPY < 200MA 이면 전 종목 신호 강도를 한 단계 낮추고
리포트 상단에 "약세 국면 · 비중 축소" 문구를 표시.

### 4-2. 한국 ETF 진입 국면 구분 (핵심)

**추세 추종과 눌림목 진입은 구조적으로 상충한다.** 추세가 강할수록 지지선에서 멀어지므로,
"강한 추세" + "지지선 근처"를 동시에 요구하면 실제로는 추세가 식은 종목만 통과한다.
따라서 하나의 진입법을 쓰지 않고, **20일선 이격도로 국면을 나눠 진입 방식 자체를 바꾼다.**

| 이격도 | 국면 | 진입 방식 | 주문 형태 | 목표가 근거 |
|---|---|---|---|---|
| ≤ 4% | **눌림목형** | 20일선 / 최근 5일 저가에 지정가 대기 | 지정가 | 관측된 저항 (10일·60일 고가, 볼린저 상단) |
| 4~12% | **돌파형** | 최근 10일 고가 돌파 시 진입 | 스탑·장중 확인 | ATR 투영 (신고가 부근이라 머리 위 저항이 없음) |
| > 12% | **과열** | 관망 | — | — |
| 20일선 아래 | **하락추세** | 관망 | — | — |

- 눌림목형 진입가는 현재가 **아래**, 돌파형 진입가는 현재가 **위**에 놓인다.
- 돌파형 목표가는 관측된 저항이 아니라 변동성 투영이므로, 리포트에 `목표근거: ATR 투영`으로 구분 표기한다.
- 이 구분이 이전 설계의 인위적인 "대기폭 5% 초과 시 탈락" 게이트를 대체한다.

### 4-3. 가격 산출식

| 항목 | 눌림목형 | 돌파형 |
|---|---|---|
| 진입가 | `min(max(20MA, 5일 저가), 종가)` | `max(10일 고가, 종가)` |
| 1차 익절 | `max(10일 고가, 진입×1.025)` | `진입 + max(1.5×ATR, 1.3×리스크)` |
| 2차 익절 | `min(볼린저상단, 60일 고가)` | `진입 + max(3.0×ATR, 2.5×리스크)` |
| 손절 | 진입 대비 **-2.5% ~ -3%** | 진입 − ATR, **-2% ~ -3.5%**로 클램프 |

모든 가격은 **호가단위(ETF: 2,000원 이상 5원)로 정렬**한다. 진입·익절은 보수적으로 내림,
손절은 보수적으로 올림 처리한다.

### 4-4. 관망 게이트 (실측 기반 조정 완료)

| 항목 | 초안 | 조정 | 근거 |
|---|---|---|---|
| 손익비 | 1.5 | **1.2** | 2~5일 스윙에 1.5는 과도. 손절 -3% 기준 익절 +3.6%면 유효 |
| 거래대금 | 10억 | **5억** | 개인 스윙 규모엔 5억이면 충분. 10억은 정상 종목까지 잘랐다 |
| 최소 상승폭 | 2.0% | **1.5%** | 1차 익절은 분할매도 지점이라 낮아도 된다 |
| 대기폭 | 5% 초과 탈락 | **삭제** | 국면 구분(눌림목/돌파)이 대체 |
| 20MA>60MA | 필수 | **완화** | "또는 20일선이 상승 중" 허용, 상승 전환 초기를 놓치지 않기 위함 |
| 미국 섹터 하락세 | 차단 | **유지** | 전략의 대전제 |

### 4-5. 갭상승 대응 (기획서 5장 2번)
오전 7시엔 한국 시가를 알 수 없으므로, 계산 대신 **각 카드에 규칙을 명시**한다:
> ⚠️ 시가가 진입가 +1.5% 위에서 열리면 진입 보류

### 4-6. 섹터 이슈 (뉴스)

구글뉴스 RSS(한국어)를 섹터 키워드로 조회한다. API 키가 필요 없고 18개 섹터 중 17개가 수확된다.

- 쿼리는 `반도체 OR 파운드리` 형태의 **OR 문법**. 단어를 나열하면 AND로 묶여 0건이 된다.
- 증시 무관 기사(채용·정책 등)는 시장 어휘 필터로 배제한다.
- 최근 3일 이내, 섹터당 최대 5건.

**긍정/부정 분류는 2단계다.**

| 단계 | 방식 | 조건 | 품질 |
|---|---|---|---|
| 기본 | 키워드 사전 | 항상 동작 | 낮음 — 문맥을 못 읽음 |
| 승격 | **Gemini** (`gemini-2.5-flash-lite`) | `GEMINI_API_KEY` 설정 시 | 높음 — 섹터 관점으로 판단 |

Gemini 경로는 SDK 없이 REST(`urllib`)로 호출해 의존성을 늘리지 않는다.
실제로 어느 쪽을 썼는지는 데이터의 `summary.classifier` 에 남고,
화면에 `AI 분류` / `키워드 분류` 로 표기된다.

**섹터를 6개씩 묶어 하루 3회만 호출한다.** 첫 배포에서 섹터당 1회씩 18회를 연속 호출했더니
앞 11개만 성공하고 뒤 7개가 전부 429 로 잘렸다(무료 티어 분당 제한 약 15 RPM).
실패 섹터를 설정 순서로 나열하면 12·13·15·16·17·18번으로 정확히 뒤쪽 전부였다.

  - 6개씩 묶어 3회 호출 → 분당 제한에서 멀어짐
  - 429/5xx 는 백오프 후 최대 3회 재시도 (`Retry-After` 존중)
  - 청크 단위 격리: 한 청크가 죽어도 나머지 청크는 AI 분류를 유지
  - **실패 이유를 로그에 남긴다.** 첫 배포에서 조용히 폴백해 원인 파악이 늦었다.

사전 방식의 한계를 완화한 조치:
- `감소/증가/확대/축소`처럼 맥락에 따라 뜻이 뒤집히는 어휘는 사전에서 제외.
  (예: "원유 재고 *감소*"는 에너지에 호재인데 사전은 부정으로 찍는다)
- 섹터 성향은 긍정·부정 기사 수가 **2건 이상 차이날 때만** 단정, 아니면 중립.
- 화면에 "참고용" 문구를 항상 노출.

가격 신호는 시세로 계산한 값이고 뉴스 태그는 **읽을거리 우선순위**일 뿐 매매 근거가 아니다.

**설정 방법**: GitHub 리포 → Settings → Secrets and variables → Actions →
`GEMINI_API_KEY` 추가. 키는 [Google AI Studio](https://aistudio.google.com/apikey)에서 발급.
설정하지 않아도 리포트는 정상 생성된다.

## 5. 웹 UI (구현 완료)

`docs/index.html` + `assets/app.js` + `assets/style.css`. 빌드 없음, 외부 CDN 없음.
JSON을 fetch 해서 렌더하는 것이 전부다.

**스크롤 순서 = 아침에 판단하는 순서**

1. 헤더 — 기준일(미국/한국 분리 표기) · 생성 시각
2. 배너 — 데이터 신선도 경고(있을 때만) + 면책 문구
3. 오늘의 핵심 전략 + 시장 국면
4. **종목 카드** (핵심)
5. 미국 섹터 20일 수익률 히트맵
6. 관망 종목 (접힘, 종목별 탈락 사유 표시)
7. 스윙 3원칙
8. 과거 리포트 선택 (2건 이상일 때만 노출)

### 종목 카드
```
HANARO 원자력iSelect              [얕은눌림형] [강한 상승]
434730
미국 원자력 (URA) 20일 +2.56% · 20일선 이격 +5.94%
┌────────────────────────────────────────────┐
│ 추세가 진행 중이라 20일선은 멉니다.            │
│ 장중 얕은 눌림에 지정가 예약을 걸어 담습니다.   │
└────────────────────────────────────────────┘
  전일 종가      61,810원
  진입 (지정가)  60,435원   -2.22%
  1차 익절       64,550원   +6.81%
  2차 익절       68,665원  +13.62%
  손절           58,325원   -3.49%
  손익비 1.95  목표근거 ATR 투영  거래대금 97.6억  돌파참고선 63,745원
  ⚠ 시가가 58,325원 아래로 열리면 예약주문을 취소하세요.
     진입가 위로 갭상승하면 체결되지 않으니 추격하지 마세요.
  [  네이버증권  ] [  토스증권  ]
  ── 섹터 이슈 · 긍정 4 / 부정 0 / 중립 1 · 키워드 분류 · 참고용
     [중립] 韓 기업, 美 현지에 원전 8기 짓는다 · hankyung.com
```

국면 칩(`얕은눌림형`/`깊은눌림형`)과 실행 안내 문구가 카드마다 붙어,
"어떻게 주문을 걸어야 하는지"가 가격보다 먼저 읽히게 했다.

### 종목 링크 (검증 완료)
| 대상 | URL 패턴 | 상태 |
|---|---|---|
| 네이버 모바일 | `https://m.stock.naver.com/domestic/stock/{코드}/total` | 화면폭 640px 이하에서 사용 |
| 네이버 PC | `https://finance.naver.com/item/main.naver?code={코드}` | 그 외 |
| 토스증권 | `https://tossinvest.com/stocks/A{코드}` | 구형(091160)·신형(0091P0) 실물 확인 |

링크 생성은 파이썬 `pipeline/links.py` 와 JS `buildLinks()` 두 곳에만 있다.
패턴이 바뀌면 그 두 곳만 고치면 된다.

### 스타일
- 모바일 우선. 카드 레이아웃, 가로 스크롤 0.
- 다크 모드 기본(`prefers-color-scheme` 추종). 새벽에 보는 화면이라 기본값이 중요하다.
- 상승 빨강 / 하락 파랑 (한국 증시 관례).
- 외부 폰트·CDN 없음 → 느린 망에서도 즉시 렌더.

### 로컬 확인
```bash
python -m http.server 8731 --directory docs
```

## 6. GitHub Actions 워크플로 (구현 완료)

`.github/workflows/daily.yml`

```yaml
on:
  schedule:
    - cron: '50 21 * * 0-4'   # KST 06:50 (월~금)
    - cron: '50 22 * * 0-4'   # KST 07:50 재시도
  workflow_dispatch:           # 수동 실행 (force 입력으로 휴장일 강제 생성)
permissions:
  contents: write
```

실행 순서: 체크아웃 → 파이썬 3.12 + pip 캐시 → 의존성 →
**계산 로직 테스트** → 리포트 생성 → 변경 있을 때만 커밋·푸시 → 요약 출력.

| 장치 | 동작 |
|---|---|
| 테스트 먼저 | 가격 계산이 깨진 채 리포트가 나가느니 아예 안 나가는 게 낫다 |
| 휴장일 스킵 | `exchange_calendars` XKRX 캘린더. 판정 불가 시엔 생성하는 쪽으로 |
| 재시도 중복 방지 | 07:50 실행은 `SKIP_IF_DONE=1`. 06:50 이 정상 생성했으면 스스로 종료 |
| 변경 없으면 커밋 안 함 | `git diff --cached --quiet` |
| 동시 실행 방지 | `concurrency: daily-report` |
| 실패 알림 | GitHub가 리포 소유자에게 자동 메일 (별도 설정 불필요) |

- GitHub Actions 크론은 최대 15분 이상 지연되는 것이 정상이다. 06:50 목표는 실제로 07:00~07:10 도착을 본다.
- **Secrets**: `GEMINI_API_KEY` (선택). 없으면 뉴스 분류가 키워드 사전으로 폴백하고 리포트는 정상 생성된다.
- **Pages**: Settings → Pages → Source: `main` 브랜치 `/docs` 폴더.
  Public 리포는 무료, Private 은 GitHub Pro 필요.

## 7. 구축 순서 (제안)

| 단계 | 내용 | 산출물 |
|---|---|---|
| ~~P0~~ | 리포 초기화, `docs/index.html` 더미, Pages 활성화 | 접속되는 URL |
| ~~P1~~ | 미국/한국 데이터 수집 + 코드 검증 + 캐시 | `cache/*.parquet` |
| ~~P2~~ | 매핑 테이블 확정 + 모멘텀 랭킹 | 섹터 랭킹 JSON |
| ~~P3~~ | 가격 산출 + 관망 게이트 + 단위 테스트 | `latest.json` 완성본 |
| ~~P4~~ | ~~웹 UI (카드 / 히트맵 / 링크 / 아카이브)~~ | **완료** |
| ~~P5~~ | ~~Actions 크론 + 공휴일 스킵~~ | **완료** |
| **P6** | (선택) 알림 발송 — 기획서 4단계의 "사용자 알림" | 텔레그램/디스코드 |

P0~P4 까지가 "매일 아침 열어보는 웹앱"의 완성 지점이고,
P5에서 사람 손을 떼며, P6는 선택이다.

---

## 8. 매핑 테이블 시드 (기획서 2단계)

> 아래 6자리 코드는 **시드값**이며, `resolve_codes.py` 가 매 실행 시 실물 대조·검증한다.

| 미국 섹터 | 한국 ETF (후보) |
|---|---|
| SOXX / SMH (반도체) | KODEX 반도체, TIGER Fn반도체TOP10 |
| XLK (기술) | TIGER 200 IT, KODEX 미국빅테크 |
| XBI / XLV (바이오·헬스케어) | KODEX 바이오, TIGER 헬스케어 |
| LIT / XLB (2차전지·소재) | KODEX 2차전지산업, TIGER 2차전지테마 |
| XLE (에너지) | KODEX 에너지화학 |
| XLF (금융) | KODEX 은행, KODEX 증권 |
| XLI / ITA (산업재·방산) | 조선·방산 테마 ETF |
| QQQ (나스닥100) | TIGER 미국나스닥100 |
| XLY (경기소비재) | KODEX 자동차 |

---

## 9. 주요 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| GH Actions 미국 IP → KRX 차단 | 한국 데이터 수집 실패 | 3단 폴백 + 캐시 + 경고 배너 |
| 크론 지연 (GH 공식 명시된 동작) | 리포트 지각 | 06:50 실행 + 07:50 재시도 |
| ETF 코드 변경·상장폐지 | 잘못된 가격 표시 | 매 실행 코드 검증, 불일치 시 실패 |
| 호가단위 미반영 | 주문 불가 가격 출력 | 5원 단위 반올림 |
| 저유동성 ETF 추천 | 체결 불가 | 거래대금 게이트 |
| 토스 URL 패턴 변경 | 링크 깨짐 | 링크 생성 1개 함수로 격리 |
